import { CurrentAdress } from "./currentAdress";

export class Hosts {
  id?: string;
  govId?: string;
  email?: string;
  phone?: string;
  currentAdress?: CurrentAdress;
  website?: string;
  isVerified?: Boolean;
}
